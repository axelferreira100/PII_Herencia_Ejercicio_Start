namespace Library
{
    public class Passenger : User
    {
        public Passenger(
            string name, string lastName, string id
        ) : base(name, lastName, id)
        {
        }
    }
}